# Plan 5 — React Client

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** React + Vite 클라이언트로 서버에 붙어 3인 게임(호스트1 + 봇2)을 실제로 플레이할 수 있게 한다 — 로비(방 생성/시작), 게임 테이블(좌석·현재 트릭·내 손패·합법수 하이라이트·카드 내기/태스크 픽), 결과. 비주얼은 기능 우선(후순위 스킨). **검증: vite 빌드 + typecheck + jsdom 통합 테스트(실제 인-프로세스 ws 서버 연동) + 실제 구동 스크린샷.**

**Architecture:** `packages/client`(Vite+React+TS). `conn.ts`가 `globalThis.WebSocket`로 서버와 JSON 메시지를 주고받고, `App`이 최신 `PlayerView`/룸 상태를 들고 화면을 라우팅한다. 합법수 하이라이트는 `legalMovesFromView`(엔진) 사용. 서버 권위 판정과 별개의 클라 프리뷰.

**Tech Stack:** React 18, Vite 5, TypeScript strict, Vitest + jsdom + @testing-library/react, `ws`(테스트에서 WebSocket 폴리필).

## Global Constraints

- 기존 워크스페이스(engine/server/shared, 111 테스트, typecheck 0) 무회귀.
- 클라이언트는 `@space-crew/shared`(프로토콜)·`@space-crew/engine`(타입·`legalMovesFromView`) 의존.
- TS strict + noUncheckedIndexedAccess. 컴포넌트는 자기 손패만 인터랙션. 합법 카드만 활성(클라 프리뷰; 최종 판정 서버).
- 비주얼 스킨은 후순위 — 최소 CSS, 레이아웃/정보 구조 우선(기존 와이어프레임 IA 따름).
- 커밋 프리픽스 `feat:`/`test:`/`chore:`. 각 태스크 끝에 커밋.

## File Structure

- `packages/client/{package.json,tsconfig.json,vite.config.ts,index.html}`
- `packages/client/src/{main.tsx,App.tsx,conn.ts,GameTable.tsx,Lobby.tsx,Card.tsx,styles.css}`
- 테스트: `packages/client/src/*.test.tsx`, `packages/client/src/integration.test.tsx`

---

## Task 1: 클라이언트 스캐폴드 (Vite+React+Vitest)

**Files:** `packages/client/{package.json,tsconfig.json,vite.config.ts,index.html}`, `src/{main.tsx,App.tsx,styles.css}`, `src/App.test.tsx`

**Interfaces:** 빌드되는 빈 React 앱 + 1개 렌더 테스트.

- [ ] **Step 1: 스캐폴드 파일 작성**

`packages/client/package.json`:
```json
{
  "name": "@space-crew/client", "version": "0.0.0", "private": true, "type": "module",
  "scripts": { "dev": "vite", "build": "tsc --noEmit && vite build", "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": { "@space-crew/engine": "*", "@space-crew/shared": "*", "react": "^18.3.1", "react-dom": "^18.3.1" },
  "devDependencies": { "@space-crew/server": "*", "@types/react": "^18.3.0", "@types/react-dom": "^18.3.0", "@vitejs/plugin-react": "^4.3.0", "vite": "^5.4.0", "vitest": "^2.0.0", "jsdom": "^25.0.0", "@testing-library/react": "^16.0.0", "@testing-library/jest-dom": "^6.4.0", "typescript": "^5.5.0", "ws": "^8.18.0" }
}
```
`packages/client/tsconfig.json`:
```json
{ "compilerOptions": { "target": "ES2022", "lib": ["ES2022", "DOM", "DOM.Iterable"], "module": "ESNext", "moduleResolution": "Bundler", "jsx": "react-jsx", "strict": true, "noUncheckedIndexedAccess": true, "esModuleInterop": true, "skipLibCheck": true, "types": ["vitest/globals", "@testing-library/jest-dom"] }, "include": ["src"] }
```
`packages/client/vite.config.ts`:
```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
export default defineConfig({
  plugins: [react()],
  test: { globals: true, environment: 'jsdom', setupFiles: ['./src/setupTests.ts'] },
});
```
`packages/client/index.html`:
```html
<!doctype html><html><head><meta charset="utf-8"><title>Space Crew</title></head><body><div id="root"></div><script type="module" src="/src/main.tsx"></script></body></html>
```
`packages/client/src/setupTests.ts`:
```ts
import '@testing-library/jest-dom';
```
`packages/client/src/styles.css`: `body{font-family:system-ui;margin:0} .legal{outline:2px solid #2b7} .dim{opacity:.4}`
`packages/client/src/main.tsx`:
```tsx
import { createRoot } from 'react-dom/client';
import { App } from './App';
import './styles.css';
const url = `ws://${location.hostname}:8787`;
createRoot(document.getElementById('root')!).render(<App serverUrl={url} />);
```
`packages/client/src/App.tsx` (stub for Task 1):
```tsx
export function App(_props: { serverUrl: string }) {
  return <div>Space Crew</div>;
}
```

- [ ] **Step 2: 렌더 테스트 작성** — `src/App.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react';
import { App } from './App';
test('App renders', () => {
  render(<App serverUrl="ws://localhost:0" />);
  expect(screen.getByText(/Space Crew/i)).toBeInTheDocument();
});
```

- [ ] **Step 3: 설치·테스트·빌드 확인** — Run: `npm install`; `npm test --workspace @space-crew/client` → PASS; `npm run build --workspace @space-crew/client` → 빌드 성공(타입 0 에러).

- [ ] **Step 4: 커밋** — `git add -A && git commit -m "chore: scaffold React client (Vite + Vitest + jsdom)"`

---

## Task 2: 서버 연결 (conn.ts)

**Files:** `packages/client/src/conn.ts`; Test `packages/client/src/conn.test.ts`

**Interfaces:**
- `interface Conn { send(msg: ClientToServer): void; close(): void }`
- `function connect(url: string, handlers: { onMessage(msg: ServerToClient): void; onOpen?(): void; onClose?(): void }): Conn` — `globalThis.WebSocket` 사용, `.onopen/.onmessage/.onclose` 핸들러. 수신은 `JSON.parse(ev.data)`로 `ServerToClient`.

- [ ] **Step 1: 실패 테스트** — `conn.test.ts` (가짜 WebSocket 주입):
```ts
import { connect } from './conn';
import type { ServerToClient } from '@space-crew/shared';

class FakeWS {
  static last: FakeWS;
  onopen: (() => void) | null = null; onmessage: ((e: { data: string }) => void) | null = null; onclose: (() => void) | null = null;
  sent: string[] = [];
  constructor(public url: string) { FakeWS.last = this; }
  send(s: string) { this.sent.push(s); }
  close() { this.onclose?.(); }
}

test('connect sends JSON and parses incoming messages', () => {
  (globalThis as any).WebSocket = FakeWS;
  const got: ServerToClient[] = [];
  const c = connect('ws://x', { onMessage: (m) => got.push(m) });
  FakeWS.last.onopen?.();
  c.send({ t: 'create', missionId: 1 });
  expect(JSON.parse(FakeWS.last.sent[0]!)).toEqual({ t: 'create', missionId: 1 });
  FakeWS.last.onmessage?.({ data: JSON.stringify({ t: 'nack', reason: 'x' }) });
  expect(got).toEqual([{ t: 'nack', reason: 'x' }]);
});
```

- [ ] **Step 2: 실패 확인** — FAIL.
- [ ] **Step 3: 구현** — `conn.ts`:
```ts
import type { ClientToServer, ServerToClient } from '@space-crew/shared';

export interface Conn { send(msg: ClientToServer): void; close(): void; }

export function connect(
  url: string,
  handlers: { onMessage(msg: ServerToClient): void; onOpen?(): void; onClose?(): void },
): Conn {
  const WS = globalThis.WebSocket;
  const ws = new WS(url);
  let open = false;
  const queue: ClientToServer[] = [];
  (ws as unknown as { onopen: () => void }).onopen = () => { open = true; handlers.onOpen?.(); for (const m of queue) ws.send(JSON.stringify(m)); queue.length = 0; };
  (ws as unknown as { onmessage: (e: { data: string }) => void }).onmessage = (e) => { handlers.onMessage(JSON.parse(e.data) as ServerToClient); };
  (ws as unknown as { onclose: () => void }).onclose = () => { handlers.onClose?.(); };
  return {
    send(msg) { if (open) ws.send(JSON.stringify(msg)); else queue.push(msg); },
    close() { ws.close(); },
  };
}
```
- [ ] **Step 4: 통과 확인** — PASS.
- [ ] **Step 5: 커밋** — `git commit -am "feat: client ws connection layer"`

---

## Task 3: 카드·게임 테이블 컴포넌트

**Files:** `packages/client/src/Card.tsx`, `packages/client/src/GameTable.tsx`; Test `packages/client/src/GameTable.test.tsx`

**Interfaces:**
- `Card.tsx`: `function CardChip({ card, dim, onClick }: { card: Card; dim?: boolean; onClick?: () => void })` — 색·값 표시, dim이면 흐리게.
- `GameTable.tsx`: `function GameTable({ view, onPlayCard, onPickTask }: { view: PlayerView; onPlayCard(c: Card): void; onPickTask(c: Card): void })` — 상단 미션/목표, 상대 좌석(이름·획득수·태스크·통신), 중앙 현재 트릭, 내 손패(합법수만 활성·`legalMovesFromView`로 판단; 클릭 시 onPlayCard). 미션 결과면 결과 표시.

- [ ] **Step 1: 실패 테스트** — `GameTable.test.tsx`:
```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { GameTable } from './GameTable';
import type { PlayerView } from '@space-crew/engine';

const view: PlayerView = {
  me: 'p0', myHand: [{ suit: 'blue', value: 7 }, { suit: 'green', value: 2 }],
  seats: [
    { player: 'p0', isBot: false, connected: true, handCount: 2, tricksWon: 0, isCommander: true, tasks: [], communication: [] },
    { player: 'p1', isBot: true, connected: true, handCount: 2, tricksWon: 1, isCommander: false, tasks: [], communication: [] },
    { player: 'p2', isBot: true, connected: true, handCount: 2, tricksWon: 0, isCommander: false, tasks: [], communication: [] },
  ],
  missionId: 1, attemptNumber: 1, phase: 'trick-in-progress',
  currentTrick: { leader: 'p0', plays: [], leadSuit: undefined },
  objectives: [], communicationPolicy: 'normal', distressActive: false, outcome: 'in-progress',
  legalMoves: [{ suit: 'blue', value: 7 }, { suit: 'green', value: 2 }],
};

test('renders my hand and fires onPlayCard for a legal card', () => {
  const onPlay = vi.fn();
  render(<GameTable view={view} onPlayCard={onPlay} onPickTask={() => {}} />);
  expect(screen.getByText(/Mission 1/i)).toBeInTheDocument();
  fireEvent.click(screen.getByTestId('hand-card-blue-7'));
  expect(onPlay).toHaveBeenCalledWith({ suit: 'blue', value: 7 });
});

test('dims illegal cards when it is not a legal move', () => {
  const v2: PlayerView = { ...view, currentTrick: { leader: 'p1', plays: [{ player: 'p1', card: { suit: 'blue', value: 9 } }], leadSuit: 'blue' }, legalMoves: undefined };
  render(<GameTable view={v2} onPlayCard={() => {}} onPickTask={() => {}} />);
  // must follow blue → blue 7 legal, green 2 dimmed
  expect(screen.getByTestId('hand-card-green-2').className).toMatch(/dim/);
});
```

- [ ] **Step 2: 실패 확인** — FAIL.
- [ ] **Step 3: 구현** — `Card.tsx` + `GameTable.tsx`. GameTable은 `legalMovesFromView(view)`(엔진)로 합법 집합을 구해 손패 카드의 활성/흐림과 클릭 핸들러를 결정. 각 손패 카드에 `data-testid={`hand-card-${suit}-${value}`}`. 상단에 `Mission {view.missionId}`. 좌석·트릭·결과 표시. (구현자는 `legalMovesFromView`를 `@space-crew/engine`에서 import.)

- [ ] **Step 4: 통과 확인** — PASS.
- [ ] **Step 5: 커밋** — `git commit -am "feat: Card and GameTable components"`

---

## Task 4: 로비 + App 라우팅·메시지 처리

**Files:** `packages/client/src/Lobby.tsx`, `packages/client/src/App.tsx`(교체); Test `packages/client/src/App.routing.test.tsx`

**Interfaces:**
- `Lobby.tsx`: `function Lobby({ room, onCreate, onStart }: { room?: RoomState; onCreate(): void; onStart(): void })` — 방 없으면 "방 만들기" 버튼, 있으면 좌석 목록 + "시작" 버튼.
- `App.tsx`: 마운트 시 `connect`. 상태 `{ room?, view? }`. 메시지: `room`→room 갱신, `view`→view 갱신, `nack`→에러 표시. `view`가 있고 phase가 트릭/배정/결과면 `GameTable`, 아니면 `Lobby`. 버튼/카드 클릭이 `send`로 액션 전송(create/start/play-card/pick-task).

- [ ] **Step 1: 실패 테스트** — `App.routing.test.tsx` (conn 모킹으로 메시지 주입; `vi.mock('./conn')` 또는 FakeWS 글로벌). room 메시지 후 "시작" 버튼이 보이고, view 메시지 후 테이블(Mission)이 보이는지 검증.
```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { App } from './App';
// FakeWS로 globalThis.WebSocket 주입 후, 서버 메시지를 흉내내 onmessage 호출
```
(구현자는 conn 레이어를 통해 들어오는 메시지를 시뮬레이션하는 테스트를 작성한다. 최소: room 수신 시 시작 버튼, view 수신 시 GameTable 렌더.)

- [ ] **Step 2: 실패 확인** — FAIL.
- [ ] **Step 3: 구현** — `Lobby.tsx` + `App.tsx`. `RoomState`는 shared의 `{t:'room'}` 페이로드 타입을 사용. App은 useEffect로 connect, useState로 room/view/error. 핸들러에서 send.
- [ ] **Step 4: 통과 확인** — PASS.
- [ ] **Step 5: 커밋** — `git commit -am "feat: Lobby and App routing with message handling"`

---

## Task 5: 엔드투엔드 통합 테스트 + 빌드 검증

**Files:** `packages/client/src/integration.test.tsx`

**목표:** 실제 인-프로세스 ws 서버(`@space-crew/server`의 `startServer`)에 클라가 붙어 방 생성→시작→테이블 렌더→(인간 차례면) 카드 한 장 내기까지 동작함을 검증.

- [ ] **Step 1: 통합 테스트 작성** — `integration.test.tsx`:
```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { WebSocket as NodeWS } from 'ws';
import { startServer } from '@space-crew/server';
import { App } from './App';

test('client connects to a real server, starts a room, and shows the table', async () => {
  (globalThis as any).WebSocket = NodeWS; // jsdom has no WebSocket
  const srv = startServer(0, { seed: 7 });
  render(<App serverUrl={`ws://127.0.0.1:${srv.port}`} />);
  // create → start (App auto-creates on connect, or via Lobby buttons)
  await waitFor(() => expect(screen.getByText(/방 만들기|시작|Mission/i)).toBeInTheDocument(), { timeout: 4000 });
  // drive the lobby if needed
  const createBtn = screen.queryByText(/방 만들기/i); if (createBtn) fireEvent.click(createBtn);
  await waitFor(() => expect(screen.queryByText(/시작/i)).toBeInTheDocument(), { timeout: 4000 });
  fireEvent.click(screen.getByText(/시작/i));
  await waitFor(() => expect(screen.getByText(/Mission/i)).toBeInTheDocument(), { timeout: 4000 });
  await srv.close();
}, 20000);
```
(구현자: App의 자동 create 여부에 맞춰 테스트 흐름을 조정. 핵심은 "실제 서버 연동으로 테이블이 뜬다"를 검증하는 것. 필요하면 App에 자동 create-on-connect를 두어 흐름을 단순화해도 됨 — 단 Lobby의 수동 흐름도 유지.)

- [ ] **Step 2: 통과 확인** — `npm test --workspace @space-crew/client` → 통합 포함 전부 PASS.
- [ ] **Step 3: 빌드·타입체크** — `npm run build --workspace @space-crew/client`(tsc+vite build) 성공, 0 에러. 다른 워크스페이스 무회귀(`npm test --workspaces`).
- [ ] **Step 4: 커밋** — `git commit -am "test: end-to-end client↔server integration + build verification"`

---

## Self-Review (작성자 점검)

- **Spec 커버리지**: spec §6.1 화면 흐름(로비→테이블→결과)·§6.2 테이블 IA(좌석·트릭·손패·합법 하이라이트)·클라 합법수 프리뷰(§4.3)를 Task 1~5가 커버. 비주얼 스킨은 후순위(합의).
- **검증**: 단위(conn/GameTable) + 통합(실제 서버 연동) + vite 빌드 + typecheck. 컨트롤러가 호스트1+봇2로 진행하므로 테이블이 실제로 뜬다.
- **타입 일관성**: `Conn`/`connect`, `CardChip`, `GameTable`, `Lobby`, `App` 시그니처 일치. 프로토콜·PlayerView는 shared/engine에서.
- **후속(Plan 6)**: 미션 선택(현재 서버 mission 1 고정), 브리핑/결과 상세, 통신/조난/커맨더 질의 UI, 캠페인 진행, 인간 합류(방 코드 입력) 흐름. 이 계획은 "기능적으로 한 판 돌아간다"에 집중.

## 다음
- Plan 6: 캠페인 영속화 + 인간 합류(방 코드 join) + 미션 선택 + 통신/조난/커맨더 UI.
